---
title: "Python"
description: "Supadata Python SDK is a wrapper around the Supadata API to help you easily turn videos into transcripts and websites into markdown."
icon: "python"
---

import InstallationPython from "/snippets/v1/installation/python.mdx";
import ScrapeAndCrawlExamplePython from "/snippets/v1/scrape-and-crawl/python.mdx";
import TranscriptPython from "/snippets/v1/transcripts/python.mdx";
import YouTubePython from "/snippets/v1/youtube/python.mdx";
import WebPython from "/snippets/v1/web/python.mdx";
import ErrorHandlingPython from "/snippets/v1/error-handling/python.mdx";

## Installation

To install the Supadata Python SDK, you can use pip:

<InstallationPython />

## Usage

Get an API key from [dash.supadata.ai](https://dash.supadata.ai), then use it with the SDK.

Here's an example of how to use the SDK:

<ScrapeAndCrawlExamplePython />

### Transcripts

Get transcript from any supported platform (YouTube, TikTok, Instagram, X (Twitter), file URLs)

<TranscriptPython />

### YouTube

Get YouTube video, channel, playlist metadata.

<YouTubePython />

### Web

Scrape and crawl web content.

<WebPython />

### Error Handling

The SDK uses custom `SupadataError` exceptions that provide structured error information:

<ErrorHandlingPython />
